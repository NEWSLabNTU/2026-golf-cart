<?xml version="1.0" encoding="UTF-8"?>
<launch>
  <!-- Minimal vehicle stack for control_test work — brings up the Turing
       Drive CAN interface and the velocity converter Autoware downstream
       expects. Works against either real (`can0`) or virtual (`vcan0`)
       SocketCAN. -->
  <arg name="vehicle_id" default="$(env VEHICLE_ID default)"/>
  <arg name="vehicle_model" default="golfcart_vehicle"/>
  <arg name="sensor_model" default="golfcart_sensor_kit"/>
  <arg name="can_interface" default="can0"/>

  <!-- Robot description (URDF + sensor_kit calibration) for TF tree. -->
  <node name="robot_state_publisher" pkg="robot_state_publisher" exec="robot_state_publisher">
    <param name="robot_description"
           value="$(command 'xacro $(find-pkg-share tier4_vehicle_launch)/urdf/vehicle.xacro vehicle_model:=$(var vehicle_model) sensor_model:=$(var sensor_model)' 'warn')"/>
  </node>

  <!-- Vehicle interface (Rust, Turing Drive CAN protocol). Topic remaps
       are defined in golfcart_vehicle_launch/launch/vehicle_interface.launch.xml,
       which we include here so this launch stays a thin shim. -->
  <include file="$(find-pkg-share golfcart_vehicle_launch)/launch/vehicle_interface.launch.xml">
    <arg name="can_interface" value="$(var can_interface)"/>
  </include>

  <!-- Vehicle velocity converter — required by Autoware localization /
       planning chain. -->
  <group>
    <push-ros-namespace namespace="sensing"/>
    <include file="$(find-pkg-share autoware_vehicle_velocity_converter)/launch/vehicle_velocity_converter.launch.xml">
      <arg name="input_vehicle_velocity_topic" value="/vehicle/status/velocity_status"/>
      <arg name="output_twist_with_covariance" value="vehicle_velocity_converter/twist_with_covariance"/>
    </include>
  </group>
</launch>
