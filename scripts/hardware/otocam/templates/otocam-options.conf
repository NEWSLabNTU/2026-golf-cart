# OTOCAM GMSL camera module options.
# max9296 deserializer requires clk_en=1 for IMX390 sensor clock.
options max9296 clk_en=1

# Block stock kernel modules from reloading via JetPack updates.
# Vendor-supplied .ko files in /usr/local/bin/otocam/ replace these.
blacklist max9295
